Small-scale demos of future work we would like to do.

Package requirements are the same as for inference_experiments, so just make a Python3.10 venv and install inference_experiments/inference_experiments_requirements.txt in the repo. 